import { CityData } from "../Classes/City";

const initialCities: CityData[] = [
  {
    text: "Junkyard",
    location: { x: 0, y: -400 },
    isCapital: false,
  },
];

export default initialCities;
